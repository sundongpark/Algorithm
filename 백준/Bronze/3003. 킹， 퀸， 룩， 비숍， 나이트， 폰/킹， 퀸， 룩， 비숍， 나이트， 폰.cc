#include <iostream>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int a;
	cin >> a;
	cout << 1 - a << ' ';
	cin >> a;
	cout << 1 - a << ' ';
	cin >> a;
	cout << 2 - a << ' ';
	cin >> a;
	cout << 2 - a << ' ';
	cin >> a;
	cout << 2 - a << ' ';
	cin >> a;
	cout << 8 - a;
	return 0;
}